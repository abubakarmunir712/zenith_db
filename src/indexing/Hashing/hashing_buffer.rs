use super::hash_bucket::HashBucket;

pub struct HashingBuffer{

   bucket:HashBucket
}
impl  HashingBuffer {
    pub fn get_bucket_ref(index:u32,db_name:String,tableID:u32,columnID:u32){
        // let mut bucket= HashBucket::new(0);
        

    }
}